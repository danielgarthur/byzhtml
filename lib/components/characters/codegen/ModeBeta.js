import { BaseMark } from '../BaseMark.js';
import { CssVars } from '../../../util/CssVars.js';

const glyphname = 'modeBeta';
const args = {};

export class ModeBeta extends BaseMark {
  constructor() {
    super(glyphname, args);
  }
}
